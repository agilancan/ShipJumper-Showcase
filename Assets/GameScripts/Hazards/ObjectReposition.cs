using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectReposition : MonoBehaviour
{
    /*
     float baseAspect;
    float currentAspect;

    private void Awake()
    {
        baseAspect = 1980f / 1080f;
        currentAspect = Screen.width / Screen.height;
        Camera.main.projectionMatrix
    }
    public Vector3 NewPosition;
    void Update()
    {
        transform.position = Camera.main.ScreenToViewportPoint(NewPosition);
        Matrix4x4.Scale(Ve)
    }
     */
}
